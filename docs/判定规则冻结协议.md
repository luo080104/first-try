# 判定规则冻结协议（2026-08-17 甲方应询书——8/25 生效）

> 甲方要求："判定规则冻结（8/25 后不再改判定逻辑——防'最后一刻调参'）"
> 状态：**协议定稿——8/25 00:00 自动生效**

---

## 一、冻结范围（8/25 起只读不改）

| 冻结对象 | 文件 | 冻结内容 |
| --------- | ------ | --------- |
| 判定逻辑 | gate_check.py | check()/significance()/_portfolio_weeks()——全部判定分支 |
| 打分体系 | strategy_score.py | THRESHOLD_MAP/权重/分项——120 制五维 |
| 信号接线 | signals.py | SIGNALS 注册表 status/wired——已启用信号集合 |
| 约束 | portfolio.py | MAX_POSITION_PCT/行业 25%/现金底线 |
| 数据口径 | portfolio.py | record_equity 三态/净值计算 |

**8/25 后允许的改动**（白名单）：

- bug 修复（经甲方确认——附复现测试）
- 数据源替换（tushare 接口变更等——不影响判定语义）
- 展示层改动（日报/周报格式）

**8/25 后禁止的改动**：

- 门槛值/权重/阈值调整
- 判定条件增删（如"加一条 XX 规则"）
- 信号启用/停用

## 二、冻结执行机制（代码强制）

### 1. 规则版本指纹（已落地——audit_judgment.py）

每次判定/审计输出规则版本：

- schema 版本号 + 门槛值 + 已接线信号集合 + 判定闸门清单

### 2. 冻结检查脚本（8/25 后每次审计比对）

```
冻结日指纹（2026-08-25 记录）vs 当前指纹：
- schema/门槛/信号集/闸门 任一变化 → 审计报告标 🔴 FROZEN-VIOLATION
- 除非改动在白名单且甲方特批（特批记录在 docs/冻结特批记录.md）
```

### 3. git 保护（可选——建议）

8/25 后 gate_check.py / strategy_score.py 的合并需人工确认：

- 提交信息必须含 `[FROZEN-OK]` 或 `[FROZEN-EXCEPTION:<理由>]`

## 三、9/12 判定流程（冻结后）

```
9/12 判定日：
1. python -m tools.strategy_engine.audit_judgment   ← 独立审计（可复跑）
2. 比对冻结指纹（8/25）——确认无 FROZEN-VIOLATION
3. gate_check.check() 出结论
4. 甲方人工复核（审计报告 + 判定结论 + 净值序列）
5. 结论落档（data/audit/ + docs/判定记录_20260912.md）
```

## 四、违约后果（协议精神）

- 冻结期内擅自改判定 → 该次判定无效（重跑冻结窗口）
- 这是"防自己骗自己"的机制——S3 白天定案晚上推翻的教训（样本外验证），冻结防止的是**判定前夜调参**这种更隐蔽的自我欺骗

---

**8/25 当天要做的**：运行一次 audit_judgment 生成冻结指纹，存入 data/audit/frozen_fingerprint_20260825.json。
