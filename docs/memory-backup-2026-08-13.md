# 记忆备份（memory target 原文快照）— 2026-08-13 合并前

> 用途：本次记忆合并（3 条 → 2 条）前的原文快照。若合并后丢失细节，以此文件为准重建。
> 原始来源：pi memory 存储 + 2026-08-13 合并 prompt。

---

## 条目 1：【Go购 全栈】

【Go购 全栈】FastAPI+SQLite+DrissionPage 比价（GitHub first-try public）：本地 :8001（老师演示）/家庭 WiFi 192.168.3.37:8001（IP 随网络变化，查 ipconfig 确认；防火墙放行）；用户端 vivo/安卓无 Chrome→依赖 localStorage/SSE；商品库 1.75万+。功能：游戏化首页（search=1 自动展开）+🚀立刻出发（session_id 全链路）+价格趋势图（Canvas 跌绿涨红）+仪表盘 5 图（ECharts 本地化）+语音搜索（Web Speech）+拍照辅助+陪你出发（AI 导购）。架构（08-13）：app.py 101 行 + routes/ 包（search 882/pages/api 811 行）+ app_state.py 共享符号（_json/_BACKGROUND_TASKS/CATEGORIES/templates），尾部 include_router 挂载；SSE 断线自动重连（3 次指数退避）。
浏览器池+采集/网络：服务启动预热 4 浏览器（9300-9303，tb/jd/vip/pdd 各一，隐藏运行），搜索/详情复用池中 tab 不销毁；严格隐藏：set.window.hide()+搜索完延迟 2s 强制 rehide+每 4 秒循环补刀（覆盖导航异步期）；探活用 CDP getVersion（不碰 tab）；headless 仅淘宝（专用副本 profile tb_profile_h，原目录不兼容）；唯品会/拼多多保留有头隐藏。弹窗事故终局（08-13 复盘）：真凶=fill_shop_founded.py 残留 while 循环（nohup+vbs 各启动一次，每 10 分钟启动浏览器爬店铺）——已清杀+单实例保护（socket 9331+装饰器 _crawl_single 覆盖 API/__main__ 双路径）；kill python≠关浏览器——残留 Edge 占端口致假风控/连接断（PageDisconnectedError），清场必须 driver.quit()/taskkill msedge.exe。服务：start_server.vbs（pythonw 完整路径+PYTHONIOENCODING=utf-8+None guard 重定向 stdout 文件，否则 print emoji 崩秒退）+开机自启。数据偏好（08-12 终版）：京东（API 榜单优先）>淘宝（headless+API，tb_profile_h）>拼多多（等 API duoId 恢复）>唯品会（有头隐藏需登录态，ZTK 过期；category.vip.com 是授权失败后的可靠替代）；headless 切换须逐平台验证（仅淘宝通）。爬虫核心原则（用户拍板勿再问）：个人自用、只读、低频 12-20s 抖动、遇验证码即停、不下单；『不绕验证码』硬底线——绕过触发平台风控（封号/IP拉黑）、影响扫码登录态致采集瘫痪、涉黑产法律风险；反爬策略：官方 API>登录态复用>低频>联系网站。采集：20h 兜底+四平台全采+词聚焦（不无限扩展新词）+ETA 实时预估+单实例锁+失败分类（验证码立即暂停/超时不计数/普通3次暂停/0结果算完成）+断点续跑（crawl_tasks fail_count+paused，resume_crawl_tasks 恢复）；crawl.py 锁装饰器覆盖 API/__main__；app.py 上限 1200 分钟；坑：函数内 import 错误可能被 except 吞掉（fill_shop _b_ 未定义）——改采集逻辑必须真实调用验证。网络：单位手机热点不稳（SSLError：采集 82% 失败、git push 需重试挂后台），回家 WiFi 恢复；手机 vivo 自带浏览器（Chromium 内核，需关省流量/智能转码）。
搜索/缓存+盯价：缓存 Reasonix v2：画像/需求卡从 system 挪 user 消息；命中率 55%→94%；llm_usage 记 cache_hit/miss（命中按 2% 计成本），首页💰KPI 80%+；统计缺失保守按全 miss。搜索历史 /hist（按天分组/点击即搜/删除/清空），/history 是价格走势接口（勿混）；INSERT OR REPLACE 需唯一索引。NL 搜索（零 LLM 纯规则分词降级）：整句 0 结果→型号 token 去停用词→组合→单 token→单中文词；结果分类：数码按类型（笔记本>整机>显卡>配件）+💻🖥🎮🔧 徽章；非数码纯价格排序；文具『笔记本』不标💻；服务类（云渲染/出租/远程）必过滤。盯价：Server酱（.env 已配）→方糖→小骆微信（企业微信 webhook 不推荐/需拉人）；notify.py 启动+每6小时定时检查、命中目标价防重复推送；定时检查+推送模式可复用（金融项目）。
学习：scripts/learn_github.py+每日 09:00 计划任务 AutoLearn-GitHub+docs/案例学习总索引.md（搜主题标记候选→自动克隆精读；案例精读须看完整 README/代码/逻辑框架，完整记录索引）。

## 条目 2：用户偏好与交互规则

用户偏好与交互规则（最高优先级，2026-08-12 确认，2026-08-14 整理并入『用户交付偏好』『用户重要偏好』『用户工作原则』『用户规则』『用户模式』等重复条目）：⚠️『永不主动提休息』与『精读≠只读』曾因记忆合并覆盖丢失过，现已固化在②⑤，勿再删。①功能效率优先、小风险可接受（原话『有点风险没事，功能效率优先』，如采集间隔 30s→15s 批准放宽）——性能/效率优先于保守风控；『不绕验证码』底线不破、『只读/不下单』合规不变；优化不得牺牲延迟（投影摘要/headroom 语义压缩已否决；RTK/context-mode 等无损省钱方案保留）。②用户没说休息之前，永远不要主动问『要不要休息/先歇会』（即『永不主动提休息』），持续干活直到用户喊停。③报告交付一律 Word（.docx），md 是中间产物，除非用户另有特殊要求（已交付示例：消费指数基金配置报告）。④「AI反向追问法」必须使用：回答前先提问→每次只问一个→据答追问→95%信心→再给方案；琐碎任务/明确指令不问、复杂技术原理不问用户、只问目标/边界/验收、不许无限提问；已固化 PI_RULES.md 规则八。⑤「精读≠只读」：案例/工具/源码能执行/下载/安装的立刻做，但必须先讲清『这是什么、装来干嘛、影响什么』经用户审核点头才动手（多次确认，最高优先级）；曾因只看不执行被批评（『不要只是看过了』）；检查历史案例落地状态是常规动作（已盘点 69 个 ref 克隆、已读未装清单）。⑥Windows bash 中文文件名坑：Python 生成 docx 放桌面会乱码（曾出乱码目录"先锋尝试"）——中文文件名必须用 PowerShell 或显式编码。⑦小骆爱刷短视频（快手/哔哩哔哩）：消遣娱乐+实用教程（编程/健身/修车等）。⑧『先共享』须含『距上次共享以来的工作』（落实情况+待办）供 WorkBuddy/小布审核；同步 SYNC.md 时追加请小布『检查有没有落下的东西』；小布（WorkBuddy）与我（小P）同一台电脑、经 SYNC.md+git 交接，小布能直接改工作区文件、我这边可见；允许小骆转发小布反馈。⑨不喜欢被打扰：不弹手机提醒、游戏化加载画面让等待变有趣、不每次复盘——关机后希望用 pi -c 或一键启动 Go购.bat 继续会话。⑩计划变更：收到『先暂停』『先不做』立即停止当前任务并记录进度快照，不强行推进；卡住太久/反复修不好先同步给小布接手，不自己硬扛。⑪时间预估必须用实测数据/ETA（实测均速×剩余量，8h/20h 只是兜底上限），讨厌拍脑袋（多次纠正）；长任务先确认总时长计划和停止条件。⑫大功能先出方案给用户确认再动手。⑬小骆会用手机实测并汇总 bug 列表，按实测结果修；反感过度自信（功能齐全≠体验好），欣赏诚实汇报（如实报失败率/异常数），随时要知道进度。⑭agents-radar 日报：项目相关内容（Go购/雕龙/观复/助手升级）必须实际精读（克隆源码→提炼→落地记录），不只停留在筛选。⑮UI 偏好：游戏化（NPC 向导/旅行地图）、首页 4 地标卡片+『陪你出发』大按钮、搜索框收进『搜全网』卡片、更多功能平铺不折叠、结果自动补搜。⑯装新 skill 必走 darwin 进化（9 维基线评估→补强失败模式编码/反例黑名单/CHECKPOINT 三件套→独立评分验证），不能裸装（已固化 PI_RULES.md 规则六）。⑰遇到不理解的新名词（如 lncrawl/headroom/Giskard）须用大白话解释，并明确是否继续（原『用户工作原则』条目并入）。⑱其余规则见 PI_RULES.md：规则十/十一（每日摘要注入/本地化/性能优化原则）。⑲AI 命名（2026-08-12 确认，并入原『用户命名』条目）：pi=小P（干活）、WorkBuddy=小布（审核）——文档/对话中一律用中文称呼，不再用 Pi/WorkBuddy 指代两者。附：投研 skill『investment-research』已用 skill-creator 沉淀（导师任务高频场景）。

## 条目 3：【记忆/编码坑】

【记忆/编码坑】记忆工具：①项目级条目（搜 [project] 前缀）存 failure 存储（按 category 归类），memory_remove 须用 target='failure'；memory 存储≠全部条目。②memory_replace 是『整条替换』——old_text 仅定位条目，匹配到子串即整条覆盖；条目内多个 § 段落会被整体覆盖丢失（2026-08-13 曾丢浏览器池/缓存/采集/编码坑段落，靠 prompt 原文重建）；§ 分隔=条目边界，段落≠条目，总条数以操作响应 entry_count 为准；合并前先 memory_search 确认边界。③合并顺序：新内容若含被删条目文本，必须先 memory_remove 再 memory_replace，否则旧子串会匹配到合并后新条目被整条误删（2026-08-13 规避）。编码：prompt/alert 裸换行与引号嵌套致脚本整块不执行→按钮全无反应；JS 改动后必须浏览器级扫描（add_init_js 9 页面全检）才能提交；bash 里 Python 写文件不落盘（py_compile 过≠已写入，必须 close 或用 edit 工具/with 语句）；run_js 调试需显式 return；print emoji 在 GBK 控制台崩（PYTHONIOENCODING=utf-8 或 reconfigure）；ruff --fix 可能删跨模块符号（如 app_state._json）——修复后必须跑真实服务验证；FastAPI 新版 include_router 是懒代理（_IncludedRouter），验证路由用 TestClient 实测不能数 routes 列表。
